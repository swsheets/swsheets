<button type="button" class="btn btn-danger <%= @extra_classes %>" data-toggle="modal" data-target="#deleteModal<%= @character.url_slug %>">Delete</button>
<div class="modal fade" id="deleteModal<%= @character.url_slug %>" tabindex="-1" role="dialog">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-body text-center">
        <p>Are you sure that you want to delete <strong><%= @character.name %></strong>?</p>
      </div>
      <div class="modal-footer">
        <button type="button" class="pull-left btn btn-default" data-dismiss="modal">Cancel</button>
        <%= link "Delete", to: character_path(@conn, :delete, @character), method: :delete, class: "btn btn-danger" %>
      </div>
    </div>
  </div>
</div>
